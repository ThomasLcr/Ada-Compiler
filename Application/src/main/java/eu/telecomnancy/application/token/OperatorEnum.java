package eu.telecomnancy.application.token;

public enum OperatorEnum {
    PLUS, //done
    MINUS, //done
    MULT, //done
    DIV, //done
    OR, //done
    OR_ELSE,
    AND, //done
    AND_THEN,
    NOT, //done
    EQUAL, // done
    NOT_EQUAL, //done
    STRICT_INF, //done
    STRICT_SUP, //done
    INF, //done
    SUP, //done
    REM, //done module
    TYPE_SPECIFIER //done ":"
}
