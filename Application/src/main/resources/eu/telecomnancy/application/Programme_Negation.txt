with Ada.Text_IO;
use Ada.Text_IO;

procedure Negation_Expression is
   Value : Integer := 10;
begin
   -- Négation de la valeur
   Value := -Value;
   -- Affichage de la valeur après la négation
   Put("Négation de la valeur ");
end Negation_Expression;
